import 'package:flutter/material.dart';

class AppStyles {
  // Marges et paddings
  static const cardPadding = EdgeInsets.all(14);

  // Titres de section
  static const sectionTitle =
      TextStyle(fontSize: 18, fontWeight: FontWeight.bold);

  // Couleurs cohérentes
  static const colorUsers = Colors.blue;
  static const colorActive = Colors.green;
  static const colorNew = Colors.orange;
  static const colorBirthday = Colors.pink;
}
